<header>
  <nav class="navbar navbar-expand-md navbar-dark  nav">
    <div class="container-fluid">
   「和仏」
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarCollapse">
        <ul class="navbar-nav me-auto mb-2 mb-md-0">
          <li class="nav-item">
            <a class="nav-link" aria-current="page" href="{{ path('home') }}">Accueil</a>
          </li>
          {% if app.user != true %}
          <li class="nav-item">
            <a class="nav-link" aria-current="page" href="/login">Connexion</a>
          </li>
          {% endif %}
          {% if app.user %}
          <li class="nav-item">
            <a class="nav-link" href="/training/moi">Mes cours</a>
          </li>
            {% endif %}
          {% if app.user %}
          <li class="nav-item">
              <a class="nav-link" aria-current="page" href="{{ path('app_logout') }}">Déconnexion</a>
          </li>
          {% endif %}
          {% if app.user %}
              {% if app.user.roles[0] == "ROLE_ADMIN" %}
          <li class="nav-item">
              <a class="nav-link" aria-current="page" href="{{ path('app_logout') }}">Gestion Admin</a>
          </li>
              {% endif %}
          {% endif %}
            <li class="nav-item">
            <a class="nav-link disabled" href="#" tabindex="-1" aria-disabled="true">
          {% if app.user %}
              {{ app.user.email }}
          {% endif %}
            </a>
            </li>
        </ul>
      </div>
      日本語会話クラブ - Wafutsu Club - Le japonais facile.
    </div>
  </nav>
</header>
