
    <div class = "row col-md">
        <div id="carouselExampleDark" class="carousel slide" data-bs-ride="carousel">
        <div class="carousel-indicators">
            <button type="button" data-bs-target="#carouselExampleDark" data-bs-slide-to="0" class="active" aria-current="true" aria-label="Slide 1"></button>
            <button type="button" data-bs-target="#carouselExampleDark" data-bs-slide-to="1" aria-label="Slide 2"></button>
            <button type="button" data-bs-target="#carouselExampleDark" data-bs-slide-to="2" aria-label="Slide 3"></button>
        </div>
        <div class="carousel-inner">
            <div class="carousel-item active" data-bs-interval="4000">
            <img src="{{ asset("build/images/cherry.jpg") }}" class="d-block w-100" alt="...">
            <div class="carousel-caption d-none d-md-block">
                <h1>PARLE JAPONAIS FACILEMENT ET COURAMMENT</h2>
                <p>Apprends à parler japonais de façon naturelle et progressive</p>
            </div>
            </div>
            <div class="carousel-item" data-bs-interval="4000">
            <img src="{{ asset("build/images/cherry.jpg") }}" class="d-block w-100" alt="...">
            <div class="carousel-caption d-none d-md-block">
                <h1>COMMUNIQUER EN JAPONAIS PAR LE JEU</h1>
                <p>Jeux de rôle et jeux de communications ludique pour parler japonais facilement</p>
            </div>
            </div>
            <div class="carousel-item">
            <img src="{{ asset("build/images/cherry.jpg") }}" class="d-block w-100" alt="...">
            <div class="carousel-caption d-none d-md-block">
                <h1>LANGUE, MOUVEMENT ET GESTUELLE</h1>
                <p>Apprends la gestuelle du japonais et la culture contextuelle du Japon moderne</p>
            </div>
            </div>
        </div>
        <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleDark" data-bs-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="visually-hidden">Previous</span>
        </button>
        <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleDark" data-bs-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="visually-hidden">Next</span>
        </button>
        </div>
    </div>
    <p class = "textuel p-4 text-center">
    Nous organisons de nombreuses actvités et cours de japonais toute l'année et pour tous les niveaux, n'hésitez pas à parcourir les cours qui vous intéressent et à vous inscrire dans
    la limite des places disponibles.
    </p>
    <p class = "title text-center">
     <stron>"がんばってね!"</strong>
    <p>
    <div class="row p-4">
        {% for training in trainings %}
            <div class="col-sm-6 mb-3">
                <div class="card pale">
                <div class="card-body">
                    <h5 class="card-title">{{ training.title }}</h5>
                    <p class="card-text">{{ training.content }}</p>
                        {% if app.user %}
                                  {% if training.spots > 0 %}
                                    {% if training.user.count == 0 %}
                                        <a href="/training/{{ training.id }}/edit" class="btn btn-primary"
                                        {% else %}
                                         <a href="/training/{{ training.id }}/edit" class="btn btn-primary disabled"
                                               {% endif %}
                                        {% else %}
                                         <a href="/training/{{ training.id }}/edit" class="btn btn-primary disabled"
                                {% endif %}
                           {% else %}
                           <a href="/training/{{ training.id }}/edit" class="btn btn-primary disabled"
                          {% endif %}
                    >
                    {{ training.spots }} places, j'y vais</a>

                 {% if training.user.count > 0 %}
                      <span class="badge  bg-success">Tu es inscrit</span>
                            {% endif %}
                </div>
                </div>
            </div>
        {% endfor %}
    </div>


                